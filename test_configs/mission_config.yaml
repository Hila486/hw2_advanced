# Sample mission configuration for testing
# All distances in centimeters

mission_config:
  max_steps: 500
  boundaries:
    x_boundary:
      min_cm: 0
      max_cm: 1000
    y_boundary:
      min_cm: 0
      max_cm: 1000
    height_boundary:
      min_cm: 0
      max_cm: 500
  gps_resolution_cm: 10
